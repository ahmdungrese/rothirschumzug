import { adminAuth } from './src/lib/firebaseAdmin';

async function test() {
  // Let's just create a custom token to test it?
  // We can't generate an ID token without a client.
  console.log("No test");
}
test();
